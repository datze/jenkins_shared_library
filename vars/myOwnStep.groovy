// creates a new "step" with the name "myOwnStep" to be used in piplines like
// myOwnStep("step 1") { ..... }

def call(STAGE_NAME, Closure closure) {

	// do something

   
    // return something if you like to
}
